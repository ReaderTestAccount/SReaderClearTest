ѕапка SReaderClearTest содержит тестовый проект SReader.
 роме стандартных дл€ андроид-проекта файлов она содержит файл SReaderCodeFormatter.xml - это экспортированный их eclips форматер кода, который примен€лс€ дл€ этого проекта.

„то сделано:
1. ќптимизирована функци€ разбити€ текста на страници;  од разбити€ вынесен в класс BookPageSpliter.java.
2. ќптимизирован парсер fb2, файл FB2ParserSaxHandler.java.

ѕриведенное решение это по сути оптимизаци€ 2-х модулей и не затрагивает архитектуры в целом. 
Ќа данный момент узким местом при открытии книги €вл€етс€ инстанциирование Chapter FB2ParserSaxHandler.endElement() . Ќа него приходитс€ пор€дка 65% вычислительного времени. ¬ нем производитс€ разбитие строки главы (chapter) на слова (создание кучи объектов String).

¬озможна значительна€ оптимизаци€ кода (как минимум на те самые 65%) если изменить архитектуру приложени€. Ќапример избавитс€ от массива слов в Book.Chapter.words. —ейчас они используютс€ только дл€ вывода на страници на экран в initView(). Ќасколько € пон€л это делаетс€ дл€ того что бы запоминать позицию (котора€ репрезентуетс€ chapterId и wordId) в тексте при выходе из книги. ќднако вместо этого можно было бы просто запоминать текущий индекс в строке chapter. Ёто бы не только сильно ускорило открытие книги но и значительно (почти в 2 раза) уменьшило бы потребление пам€ти, что в свою очередь позволит открывать книги большего размера.

